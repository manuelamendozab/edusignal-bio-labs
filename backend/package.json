{
  "name": "edusignal-bio-labs-backend",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "node index.js",
    "start": "node index.js",
    "build": "echo \"No build step for backend\"",
    "init-db": "node scripts/init-db.js",
    "reset-db": "rm -f database.sqlite && npm run init-db"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.20.0",
    "better-sqlite3": "^9.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.4"
  }
}
